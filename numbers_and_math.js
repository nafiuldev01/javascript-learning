const number = 400;

// console.log(number);

const otherNumber = new Number(100);

const floatNumber = 123.84722;

// console.log(floatNumber.toFixed(2));

const min = 1;
const max = 6;

console.log(Math.floor(Math.random() * (max - min + 1) + min));
