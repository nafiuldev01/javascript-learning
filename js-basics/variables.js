// var,const,let and wihtout any keywords we can declare variable

var name = "Nafi"; // ! var is function scoped...which is a issue for modern day programming so stop using var. var can be redaclared
let anotherName = "Rafi"; // ! respects scope...can be redefined
const otherName = "wahid"; // ! cannot redefine it...const respect scope

// motherName = "Shiuli"; // ❌ not use it

// console.log(name);
// console.log(anotherName);
// console.log(otherName);
